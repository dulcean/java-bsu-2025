services:
  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - db

  db:
    image: postgres
    environment:
      POSTGRES_DB: minilab
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
